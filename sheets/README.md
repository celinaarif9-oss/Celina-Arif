# sheets/ — Google Sheets templates & Apps Script

The human-facing surface the team uses daily: the "Daily Orders" production sheet
(Phase 1, item 2), the pricing calculator (item 4 — see `../finance/pricing-calculator/`),
and any Apps Script buttons (e.g. mark items *in production / done*).

**Rules**
- Sheets the team edits live in their Google account; this folder holds the **templates,
  schemas, and Apps Script source** kept in version control.
- No real customer PII committed here — use anonymised sample rows.

*Empty for now — populated when the Make.com order-sheet sync (item 2) is built.*
